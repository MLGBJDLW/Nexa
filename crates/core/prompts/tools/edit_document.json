{
  "name": "edit_document",
  "description": "Edit an existing Office document (DOCX, PPTX, XLSX) by performing targeted text replacements. Opens the document's internal XML, finds and replaces specified text, and saves the modified document. Preserves all formatting, images, styles, and elements not involved in the replacement.\n\nUse this tool for quick/simple Office text replacement. For richer Office workflows (create from templates, bulk edits, structured insertion, redaction, snapshot/version) prefer run_shell + doc-script-editor (Python libraries).\n\nHandles the split-runs problem where Office XML splits visible text across multiple XML elements.\n\nFor redaction, slide insertion, PDF editing, or diff-preview/snapshotting, use run_shell with the doc-script-editor skill instead — this tool only does plain text replace.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the existing Office document (absolute or relative to a source directory)"
      },
      "replacements": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "old_text": {
              "type": "string",
              "description": "Exact text to find in the document"
            },
            "new_text": {
              "type": "string",
              "description": "Text to replace it with"
            }
          },
          "required": ["old_text", "new_text"]
        },
        "description": "List of text replacements to apply. Each replacement finds exact text in the document and replaces it."
      }
    },
    "required": ["path", "replacements"]
  }
}
