{
  "name": "create_file",
  "description": "Create a new file at the specified path within a registered source directory. Parent directories are created automatically. Supports text-based formats (md, txt, json, csv, rs, py, etc.).\n\nUse this tool when:\n- You need to create a brand-new file at an arbitrary path within a source directory\n- The file does not yet exist (or you explicitly set overwrite to true)\n\nFor editing existing files, use edit_file instead.\nFor notes in the notes/ subdirectory, use write_note instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the file to create (absolute or relative to a source directory). Must be within a registered source directory."
      },
      "content": {
        "type": "string",
        "description": "The text content to write to the file."
      },
      "overwrite": {
        "type": "boolean",
        "description": "If true, overwrite the file if it already exists. Default: false."
      }
    },
    "required": ["path", "content"]
  }
}
