{
  "name": "summarize_evidence",
  "description": "Retrieve and format specific evidence cards by their chunk IDs for citation. Returns the chunk content together with source path and document title.",
  "parameters": {
    "type": "object",
    "properties": {
      "chunk_ids": {
        "type": "array",
        "items": { "type": "string" },
        "description": "List of chunk IDs to retrieve and format"
      }
    },
    "required": ["chunk_ids"]
  }
}
