{
  "name": "fetch_url",
  "description": "Fetch and read the text content of a web page. Use this only when the user shares a URL or explicitly asks for web content. Returns the stripped page text plus URL/title provenance and a suggested citation.",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to fetch (must be http:// or https://)."
      },
      "max_length": {
        "type": "integer",
        "description": "Maximum number of characters to return. Default: 5000."
      }
    },
    "required": ["url"]
  }
}
