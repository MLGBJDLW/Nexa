{
  "description": "Execute a command with platform-aware safety controls. You can pass either `command` for short one-line commands like `git status --short`, or `program` plus `args` for exact argv control. Without a saved shell selection, plain `command` input is parsed into exact argv without a shell. In ConfirmAll/Open modes, recognizable shell syntax automatically uses the configured default shell; Restricted mode continues to reject it. In ConfirmAll/Open, a saved shell environment also applies to plain command strings. Exact program/args stays on the host. Set `shell` explicitly to override the saved environment. Shell execution is rejected in Restricted access and only works in ConfirmAll/Open. In the default restricted mode, only whitelisted commands are allowed: python/python3/pip/pip3/node/npm/npx, read-only git, and scoped filesystem commands like pwd/ls/cat/mkdir/cp/mv. Simple filesystem commands are implemented natively by the app, so use them directly for directory creation, copying, and moving instead of writing one-off Python snippets. For plain-text read/create/edit/list work, prefer read_file, read_files, create_file, edit_file, and list_dir over Python. Reserve Python for real scripts, structured document workflows, parsing/transforms, or operations that need libraries. For large scripts or generated text, pass the content through `stdin` and use a program form that reads stdin (for example python with args [\"-\"]); do not stuff large content into argv. If a disposable script or intermediate file must be saved, place it under <active-workspace>/.nexa/tmp/<task>/, even when cwd is a nested directory. Keep maintained source and user deliverables at their intended paths; honor explicit user paths. For HTML-first PPTX/deck generation, call the renderer with `--spec -` and put the generated JSON spec in `stdin`; never put raw HTML/CSS/JSON deck specs inside `args` or `python -c`. Output is capped at 64 KB per stream. External commands without stdin are observed briefly, then automatically detached if still running; callers do not predict duration. Follow the returned activityId/cursor with activity_observe: waitFor=completion keeps builds/tests attached for up to 60 seconds per call; output mode waits up to 2.5 seconds. Native filesystem operations and stdin-driven commands remain finite foreground runs with default timeout 30s. For Windows paths in JSON strings, escape backslashes (for example \"E:\\\\Starting\\\\script.py\") or use forward slashes. For desktop computer use, launch source-scoped executables with desktop_automation action=launch_app, path and args. Shell child processes (including cmd start and Start-Process) belong to the managed process tree and may be terminated when the command completes; launch_app keeps the desktop application alive independently.",
  "name": "run_shell",
  "parameters": {
    "properties": {
      "args": {
        "default": [],
        "description": "Arguments passed directly as argv when using program (no shell interpretation). Defaults to []. Do not use args with command. For generated HTML/PPTX specs, use args like [\".../edit_doc.py\", \"--path\", \".../deck.pptx\", \"create_html_pptx\", \"--spec\", \"-\", \"--outdir\", \".../html_deck_project\", \"--mode\", \"hybrid\", \"--screenshot\", \"auto\"] and put the JSON spec in stdin. In restricted mode, git first arg must be a read-only subcommand (status, diff, log, show, ls-files, rev-parse, branch, tag, config, remote, describe, blame), and filesystem-command paths are resolved relative to cwd and must stay inside registered source roots.",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "background": {
        "default": false,
        "deprecated": true,
        "description": "Deprecated compatibility hint. External commands without stdin are observed briefly and automatically detached when still running; callers never need to predict whether a command will be long-lived.",
        "type": "boolean"
      },
      "command": {
        "description": "Command string. Without a saved shell selection, plain commands are parsed into exact argv without a shell. In ConfirmAll/Open modes, shell syntax such as ;, &&, |, redirection, command substitution, or multiline scripts automatically uses the configured default shell; Restricted mode still rejects shell syntax. You may also set shell explicitly. On Windows, path backslashes are preserved by the command parser; JSON strings still need escaped backslashes such as \"C:\\\\Users\\\\me\\\\script.py\". Do not provide command together with program or args.",
        "type": "string"
      },
      "cwd": {
        "description": "Optional working directory. Defaults deterministically to the first source in the active conversation scope. In restricted mode it must resolve inside a registered source directory; less-restricted modes may use any existing directory.",
        "type": "string"
      },
      "program": {
        "description": "Program to execute when not using command. In restricted mode it must be one of: python, python3, pip, pip3, node, npm, npx, git, pwd, ls, cat, mkdir, cp, mv. pwd/ls/cat/mkdir/cp/mv run natively inside the app and work even when the OS has no matching external binary. pip/pip3 are normalized to python/python3 -m pip. Simple aliases copy->cp and move->mv are also accepted. In ConfirmAll/Open modes, use a command name or an absolute executable path, including paths containing spaces. Restricted mode still requires a whitelisted bare name. No shell interpreter is invoked automatically.",
        "type": "string"
      },
      "ready_timeout_secs": {
        "default": 20,
        "deprecated": true,
        "description": "Deprecated compatibility field. Startup observation is bounded to about 1.5 seconds; use activity_observe or service_action=wait for incremental readiness events.",
        "maximum": 120,
        "minimum": 1,
        "type": "integer"
      },
      "ready_url": {
        "description": "Optional loopback HTTP(S) URL used to verify a background service, for example http://127.0.0.1:4173. Only localhost and loopback IP addresses are accepted. When omitted, run_shell may infer an exact endpoint declared by a recognized server invocation or discover a full loopback URL in startup logs. Bare port text alone never grants browser network access; pass ready_url when the endpoint is otherwise ambiguous.",
        "type": "string"
      },
      "service_action": {
        "default": "run",
        "description": "Use status, wait, or stop with a service_id returned by a detached run. status takes one snapshot. wait blocks until completion or its observation budget expires (default 10 seconds, maximum 60 seconds); a live timed wait is progress, not a repeated-call error. Prefer activity_observe with a cursor for incremental events. Omit for ordinary commands.",
        "enum": [
          "run",
          "status",
          "wait",
          "stop"
        ],
        "type": "string"
      },
      "service_id": {
        "description": "Managed service identifier returned by a background run. Required for service_action status, wait, or stop.",
        "type": "string"
      },
      "shell": {
        "default": false,
        "description": "Optional explicit shell mode for command. false/none/direct means no shell. true/default uses PowerShell on Windows and sh on Unix-like systems. powershell uses Windows PowerShell on Windows or pwsh elsewhere; pwsh uses PowerShell Core; cmd is Windows-only; bash uses bash -lc; sh uses sh -c. Shell mode permits shell syntax such as &&, pipes, redirects, variables, and command substitution, but is rejected in Restricted shell access mode and should only be used when shell interpretation is intentional.",
        "oneOf": [
          {
            "type": "boolean"
          },
          {
            "enum": [
              "none",
              "default",
              "powershell",
              "pwsh",
              "cmd",
              "bash",
              "sh"
            ],
            "type": "string"
          }
        ]
      },
      "stdin": {
        "description": "Optional text written to the child process stdin. Use this for scripts or generated content that would exceed argv limits, e.g. program python with args [\"-\"] and stdin containing the script. For HTML-first PPTX generation, pass the JSON deck spec here while using --spec - in args. The payload is bounded and is not logged. Native filesystem commands do not accept stdin.",
        "type": "string"
      },
      "timeout_secs": {
        "default": 30,
        "description": "Timeout in seconds for a finite foreground run (native filesystem operations or commands using stdin). Default 30. External commands without stdin automatically detach if still running. With service_action=wait this is an observation budget (default 10 seconds, maximum 60 seconds); it does not terminate the process.",
        "minimum": 0,
        "type": "integer"
      }
    },
    "required": [],
    "type": "object"
  }
}
